# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# A run's output: the user's own schema, ontology and review queue.
erp-planner/

# Benchmark working data (large, regenerable, or customer-confidential)
data/runs/
data/private/
*.sqlite
*.db

# Generated graph renders (regenerate with `mise run graph`)
*.dot

# OS / editor
.DS_Store
.idea/
.vscode/

# RODI benchmark artefacts: schemas, predictions and scores regenerated by
# `mise run bench:rodi <rodi-clone>`. Outputs, not source — the numbers they
# produced are recorded in docs/rodi.md.
data/benchmarks/rodi/
